import 'package:flutter/material.dart';

class MainMobileBody extends StatelessWidget {
  const MainMobileBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
